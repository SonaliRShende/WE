import { BriefcaseBusiness, Building2, ChevronRight } from "lucide-react";
import { useNavigate } from "react-router-dom";
import Footer from "../components/Footer";
import Navbar from "../components/Navbar";
import { useLocale } from "../context/LocaleContext";
import jobProviderImg from "../assets/job-provider.jpg";
import jobSeekerImg from "../assets/job-seeker.jpg";

export default function Home() {
  const navigate = useNavigate();
  const { messages } = useLocale();

  const cards = [
    {
      image: jobSeekerImg,
      title: messages.home.seekerTitle,
      body: messages.home.seekerBody,
      action: messages.home.seekerAction,
      icon: BriefcaseBusiness,
      accent: "sky",
      onClick: () => navigate("/job-seeker-dashboard"),
    },
    {
      image: jobProviderImg,
      title: messages.home.providerTitle,
      body: messages.home.providerBody,
      action: messages.home.providerAction,
      icon: Building2,
      accent: "indigo",
      onClick: () => navigate("/job-provider-dashboard"),
    },
  ];

  return (
    <div className="flex min-h-screen flex-col">
      <Navbar />
      <main className="flex-1 px-4 py-12 sm:px-6">
        <div className="mx-auto max-w-7xl">
          <section className="rounded-[2.25rem] bg-[linear-gradient(125deg,#0f172a,#10203d,#2563eb)] px-6 py-12 text-white shadow-[0_35px_80px_-45px_rgba(15,23,42,0.8)] sm:px-10">
            <p className="text-sm font-semibold uppercase tracking-[0.24em] text-sky-200">
              {messages.home.eyebrow}
            </p>
            <h1 className="mt-4 max-w-4xl text-4xl font-semibold leading-tight sm:text-5xl">
              {messages.home.title}
            </h1>
            <p className="mt-5 max-w-3xl text-lg leading-8 text-sky-100">
              {messages.home.description}
            </p>
          </section>

          <section className="mt-10 grid gap-6 lg:grid-cols-2">
            {cards.map((card) => {
              const Icon = card.icon;
              const accentClasses =
                card.accent === "sky"
                  ? {
                      chip: "bg-sky-50 text-sky-700",
                      action: "text-sky-700",
                    }
                  : {
                      chip: "bg-indigo-50 text-indigo-700",
                      action: "text-indigo-700",
                    };

              return (
                <button
                  key={card.title}
                  type="button"
                  onClick={card.onClick}
                  className="group overflow-hidden rounded-[2rem] border border-slate-200 bg-white/90 text-left shadow-[0_24px_60px_-40px_rgba(15,23,42,0.35)] transition hover:-translate-y-1 hover:shadow-[0_30px_80px_-40px_rgba(15,23,42,0.45)]"
                >
                  <div className="aspect-[16/10] overflow-hidden">
                    <img
                      src={card.image}
                      alt={card.title}
                      className="h-full w-full object-cover transition duration-700 group-hover:scale-105"
                    />
                  </div>
                  <div className="p-7">
                    <div className={`inline-flex rounded-2xl p-3 ${accentClasses.chip}`}>
                      <Icon size={22} />
                    </div>
                    <h2 className="mt-5 text-3xl font-semibold text-slate-950">
                      {card.title}
                    </h2>
                    <p className="mt-4 text-base leading-7 text-slate-600">{card.body}</p>
                    <div className={`mt-6 inline-flex items-center gap-2 text-sm font-semibold uppercase tracking-[0.18em] ${accentClasses.action}`}>
                      {card.action}
                      <ChevronRight size={16} className="transition group-hover:translate-x-1" />
                    </div>
                  </div>
                </button>
              );
            })}
          </section>

          <p className="mt-10 text-center text-sm leading-7 text-slate-500">
            {messages.home.footerNote}
          </p>
        </div>
      </main>
      <Footer />
    </div>
  );
}
